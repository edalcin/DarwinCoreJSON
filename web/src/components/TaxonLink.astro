---
import { getTaxon } from '../lib/mongo'
const { taxonId } = Astro.props
const taxon = await getTaxon(taxonId)
---

<a href={`/taxon/${taxonId}`} class="text-green-800 underline"
  >{taxon?.scientificName ?? '??'}</a
>
