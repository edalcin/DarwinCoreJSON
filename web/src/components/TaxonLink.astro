---
import { getTaxon } from '../lib/mongo'
const { kingdom, taxonId } = Astro.props
const taxon = await getTaxon(kingdom, taxonId)
---

<a
  href={`/taxon/${kingdom[0].toUpperCase()}${taxonId}`}
  class="text-green-800 underline">{taxon?.scientificName ?? '??'}</a
>
